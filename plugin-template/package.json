{
  "name": "plugin-template",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "tsc && node scripts/bundle.js"
  },
  "devDependencies": {
    "@micropress/plugin-sdk": "^0.1.0",
    "@tiptap/core": "^2.1.0",
    "typescript": "^5.3.0",
    "esbuild": "^0.19.0"
  }
}
